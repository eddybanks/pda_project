<% @header="Attractions" %>
<div class="panel-body">
  <div class="row">
    <div class="col-md-12">
      <%= render 'layouts/search' %>
      <h2><%= link_to 'Add Column', new_attraction_path, class: "btn btn-primary" %></h2>
      <div id="attractiontable" class="table-responsive">

        <table class="table table-striped">

          <thead>

            <th><input type="checkbox" class="checkall" /></th>
            <th>Number</th>
            <th>Name</th>
            <th>Size</th>
            <th>Type</th>
            <th>Park_number</th>
            <th colspan='3'>Actions</th>
          </thead>

          <tbody>

            <% @attractions.each do |attraction| %>
            <tr>
              <td><input type="checkbox" class="checkthis" /></td>
              <td><%= attraction.id %></td>
              <td><%= attraction.name %></td>
              <td><%= attraction.size %></td>
              <td><%= attraction.category %></td>
              <td><%= attraction.park_number %></td>
              <td><p data-placement="top" data-toggle="tooltip" title="Show"><button class="btn btn-primary btn-xs" data-title="Show" data-toggle="modal" data-target="#show" ><span class="glyphicon glyphicon-open"></span></button></p></td>
              <td><p data-placement="top" data-toggle="tooltip" title="Edit"><%= link_to raw('<span class="glyphicon glyphicon-pencil"></span>'), edit_attraction_path(attraction), class: "btn btn-primary btn-xs" %></p></td>
              <td><p data-placement="top" data-toggle="tooltip" title="Delete"><button class="btn btn-danger btn-xs" data-title="Delete" data-toggle="modal" data-target="#delete" ><span class="glyphicon glyphicon-trash"></span></button></p></td>
            </tr>

            <% end %>

          </tbody>

        </table>

        <%= will_paginate @attractions %>

      </div>
    </div>
  </div>
</div>
